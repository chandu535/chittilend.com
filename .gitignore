node_modules
.DS_Store
dist
dist-ssr
*.local
# Every env file, not just the bare one — .env alone does not match .env.prod
.env
.env.*
!.env.example
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Vercel build output — regenerated on every deploy
.vercel

# Extracted legacy ledger — contains borrower PII and photographs
.extract
